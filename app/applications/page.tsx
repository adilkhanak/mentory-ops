import { AppShell } from "@/components/app-shell";
import { ApplicationTable } from "@/components/application-table";
import { NewCandidateForm } from "@/components/new-candidate-form";
import { getActor, listApplications } from "@/lib/server";

export const dynamic = "force-dynamic";
export default async function ApplicationsPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) {
  const params=await searchParams; const actor=await getActor("/applications"); const rows=await listApplications(params.q??"",params.course??"",params.status??"",params.cv??"",params.motivation??"",params.interview??"",params.decision??"");
  return <AppShell actor={actor}><div className="space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-blue-700">Единый реестр</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Кандидаты</h1><p className="mt-2 text-sm text-slate-500">Поиск, фильтры и все этапы отбора в одной таблице.</p></div><a href="/api/export" className="rounded-xl border border-slate-200 bg-white px-4 py-2 text-center text-sm font-semibold hover:bg-slate-50">Экспорт CSV</a></div>{["ADMIN","MANAGER"].includes(actor.role)&&<NewCandidateForm/>}<form className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4"><input name="q" defaultValue={params.q} placeholder="ФИО, email или ИИН" className="h-10 rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-500"/><select name="course" defaultValue={params.course} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Все курсы</option><option value="junior-frontend">Junior Frontend</option><option value="middle-frontend">Middle Frontend</option></select><select name="cv" defaultValue={params.cv} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Любой статус CV</option><option value="NOT_REVIEWED">CV не проверено</option><option value="APPROVED">CV одобрено</option><option value="MAYBE">CV в резерве</option><option value="REJECTED">CV отклонено</option></select><select name="motivation" defaultValue={params.motivation} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Любая мотивация</option><option value="NOT_SENT">Не запрошена</option><option value="SENT">Запрошена</option><option value="RECEIVED">Получена</option></select><select name="interview" defaultValue={params.interview} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Любое интервью</option><option value="NOT_SCHEDULED">Не назначено</option><option value="SCHEDULED">Назначено</option><option value="ATTENDED">Проведено</option><option value="NO_SHOW">Не пришёл</option></select><select name="decision" defaultValue={params.decision} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Любое решение</option><option value="PENDING">Решение не принято</option><option value="ACCEPTED">Принят</option><option value="WAITLIST">Резерв</option><option value="REJECTED">Отказ</option></select><select name="status" defaultValue={params.status} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">Любой этап заявки</option><option value="NEW">Новая</option><option value="IN_REVIEW">В работе</option><option value="APPROVED">Одобрена</option><option value="REJECTED">Отклонена</option></select><div className="flex gap-2"><button className="h-10 flex-1 rounded-xl bg-[#172554] px-4 text-sm font-semibold text-white">Применить</button><a href="/applications" className="grid h-10 place-items-center rounded-xl border border-slate-200 px-4 text-sm font-semibold">Сбросить</a></div></div></form><ApplicationTable rows={rows as Array<Record<string, any>>}/></div></AppShell>;
}
