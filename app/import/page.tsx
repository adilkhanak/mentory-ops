import { AppShell } from "@/components/app-shell"; import { ImportPanel } from "@/components/import-panel"; import { database, getActor } from "@/lib/server";
export const dynamic="force-dynamic";
export default async function ImportPage(){const actor=await getActor("/import");const history=(await database().prepare("SELECT * FROM import_runs ORDER BY created_at DESC LIMIT 15").all()).results as Array<Record<string,any>>;return <AppShell actor={actor}><div className="space-y-5"><div><p className="text-sm font-semibold text-blue-700">Официальный реестр</p><h1 className="mt-1 text-3xl font-bold">Импорт заявок</h1><p className="mt-2 max-w-3xl text-sm text-slate-500">Повторная загрузка обновляет существующих кандидатов по ИИН или email и не создаёт дубликаты.</p></div><ImportPanel/><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">История импортов</h2>{history.length?<div className="mt-3 divide-y divide-slate-100">{history.map(r=><div key={r.id} className="flex flex-wrap items-center justify-between gap-3 py-3 text-sm"><div><p className="font-semibold">{r.source_name}</p><p className="text-xs text-slate-500">{new Date(r.created_at).toLocaleString("ru-RU")}</p></div><p className="tabular-nums text-slate-500">{r.rows_imported} из {r.rows_total}</p></div>)}</div>:<p className="mt-6 text-sm text-slate-500">Импортов пока не было.</p>}</section></div></AppShell>}
